import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

export default function LastRecord(){
  return(
    <View>
      <Text>Last Records Overview</Text>
    </View>
  )
};

const styles = StyleSheet.create({});